#include_jtlib <int_list.jt>

func int main()
{
	struct int_list *header;
	header = initialize();
	insert(header,2);
	insert(header,2);
	insert(header,3);
	insert(header,9);
	insert(header,100);
	insert(header,61);

	if (ilist_contains(header,100) == true) {
		print("passed contains test");
	}

	return 0;
}
